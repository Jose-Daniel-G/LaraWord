# version: '3.8'

services:

  # -------------------------
  # PostgreSQL
  # -------------------------
  db:
    image: postgres:15-alpine
    container_name: liberty-db-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - cars_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # -------------------------
  # Laravel (PHP-FPM)
  # -------------------------
  laravel_app:
    build: .
    container_name: laravel_app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cars_net
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      DB_HOST: db
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}

  # -------------------------
  # Nginx
  # -------------------------
  nginx:
    image: nginx:alpine
    container_name: nginx_car
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - laravel_app
    networks:
      - cars_net

  # mailhog:
  #   image: mailhog/mailhog
  #   ports:
  #     - "8025:8025"
  #   networks:
  #     - cars_net

  # -------------------------------------------------------
  # SERVICIOS OBSOLETOS / COMENTADOS (MySQL)
  # -------------------------------------------------------
  # mysql:
  #   image: mysql:8.1.0
  #   container_name: liberty-container
  #   environment:
  #     MYSQL_DATABASE: ${DB_DATABASE}
  #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
  #     MYSQL_USER: ${DB_USERNAME}
  #     MYSQL_PASSWORD: ${DB_PASSWORD}
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   networks:
  #     - cars_net
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 10

  # phpmyadmin:
  #   image: phpmyadmin/phpmyadmin:latest
  #   container_name: phpmyadmin_cars
  #   environment:
  #     PMA_HOST: mysql
  #     PMA_PORT: 3306
  #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
  #   ports:
  #     - "8081:80"
  #   depends_on:
  #     - mysql
  #   networks:
  #     - cars_net

# -------------------------------------------------------
# VOLÚMENES Y REDES
# -------------------------------------------------------


volumes:
  pg_data:
  # mysql_data: # Lo mantenemos por si quieres recuperar los datos viejos después

networks:
  cars_net:
    driver: bridge
